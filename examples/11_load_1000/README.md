# 1000-row load example

Longer generation/load fixture: 1000 rows with a concurrency width of 32.
This is deliberately not part of the default test run.

```sh
jobchain run config.yaml --no-submit
```
