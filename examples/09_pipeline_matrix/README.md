# Pipeline topology example

A five-stage linear pipeline demonstrating command stages, explicit class
resolution, dependency types, per-stage resources, environment variables,
and a non-final chaining stage.

Run with a scheduler or inspect generated scripts with:

```sh
jobchain run config.yaml --no-submit
```
