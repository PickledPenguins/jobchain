# Format example

Demonstrates CSV quoting, delimiters embedded in values, escaped quotes, and
normal scalar validation.

```sh
jobchain run config.yaml --check
jobchain run config.yaml --no-submit
```
